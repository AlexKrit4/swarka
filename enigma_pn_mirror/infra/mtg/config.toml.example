# Пример конфига mtg (Fake-TLS)
# Secret сгенерируйте: docker run --rm nineseconds/mtg:2 generate-secret --hex www.google.com
# и пропишите ниже + в корневой .env как MTPROTO_SECRET

secret = "REPLACE_WITH_EE_SECRET"
bind-to = "0.0.0.0:3128"
